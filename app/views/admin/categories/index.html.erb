<h1>Категории</h1>
<table class="table">
  <tr>
    <th>Название</th>
    <th></th>
    <th>Название</th>

  </tr>
  <% @categories.each do |category| %>
    <tr>
      <td><%= link_to category.name, category %></td>
      <td><%= link_to 'edit', edit_admin_category_path(category)%></td>
      <td><%= link_to 'delete', [:admin, category], method: :delete %></td>
    </tr>
  <% end %>
</table>

<% if signed_in? %>
    <div class="category-control-add"><%= link_to "Создать категорию", new_admin_category_path, :class => 'btn'%></div>


   <% end %>

