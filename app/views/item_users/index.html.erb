<h1>Корзина</h1>

<% if @item_users.any? %>
<table class = "table">
  <tr>
    <td>Название товара</td>
    <td>Цена</td>
    <td>Количество</td>
    <td></td>

  </tr>
  <% @item_users.each do |t| %>
  <tr>
    <td><%= t.item.name %></td>
    <td><%= t.item.price %></td>
    <td><%= t.quantity %></td>
    <td><%= link_to 'Удалить',t, method: :delete, confirm: 'Точно?' %> </td>
  </tr>


<% end %>
</table>
   <%= link_to 'Оформить заказ', orders_path, method: :post, class: 'btn btn-success btn-large' %>

 <% else %>
    <p>У вас в корзине ничего нет, сходите что-нибудь купите.</p>
<% end %>
